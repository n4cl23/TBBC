import {NextRequest} from 'next/server';const windows=new Map<string,{count:number;reset:number}>();export function requestContext(request:NextRequest){return {actor:process.env.ADMIN_USER||'development-admin',ip:request.headers.get('x-forwarded-for')?.split(',')[0]?.trim()||'local'}}export function validateAdminMutation(request:NextRequest){const origin=request.headers.get('origin'),host=request.headers.get('host');if(origin&&host&&!origin.includes(host))return 'Origem da requisição não permitida.';const key=request.headers.get('x-forwarded-for')||'local',now=Date.now(),entry=windows.get(key);if(!entry||entry.reset<now){windows.set(key,{count:1,reset:now+60_000});return null}entry.count++;if(entry.count>60)return 'Limite de alterações excedido. Tente novamente em um minuto.';return null}
