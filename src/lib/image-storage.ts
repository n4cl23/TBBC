import 'server-only';import {mkdir,unlink} from 'node:fs/promises';import path from 'node:path';import crypto from 'node:crypto';import sharp from 'sharp';import type {Metadata} from 'sharp';import type {MediaImage,MediaVariant} from '@/types/media';import {validateImageInput} from './media';
export interface ImageStorageProvider{upload(file:File,scope:string,primary:boolean,alt:string):Promise<MediaImage>;delete(image:MediaImage):Promise<void>;replace(oldImage:MediaImage,file:File,scope:string,alt:string):Promise<MediaImage>;list(scope:string):Promise<MediaImage[]>;getPublicUrl(relativePath:string):string}
const sizes:Record<MediaVariant,number>={thumbnail:400,card:800,full:2000};
export class LocalImageStorageProvider implements ImageStorageProvider{getPublicUrl(relativePath:string){return `/${relativePath.replaceAll('\\','/')}`}async upload(file:File,scope:string,primary:boolean,alt:string){const validation=validateImageInput(file);if(validation)throw new Error(validation);const bytes=Buffer.from(await file.arrayBuffer());let metadata:Metadata;try{metadata=await sharp(bytes).metadata()}catch{throw new Error('Não foi possível processar o arquivo.')}if(!metadata.width||!metadata.height)throw new Error('A imagem parece estar corrompida.');const safeScope=scope.toLowerCase().replace(/[^a-z0-9/_-]/g,'-');const id=crypto.randomUUID(),kind=primary?'primary':'gallery',folder=path.join(process.cwd(),'public','uploads',safeScope,kind,id);await mkdir(folder,{recursive:true});const variants:Partial<Record<MediaVariant,string>>={};for(const [variant,width] of Object.entries(sizes) as [MediaVariant,number][]){const relative=path.join('uploads',safeScope,kind,id,`${variant}.webp`);await sharp(bytes).rotate().resize({width,withoutEnlargement:true}).webp({quality:88}).toFile(path.join(process.cwd(),'public',relative));variants[variant]=this.getPublicUrl(relative)}const now=new Date().toISOString();return {id,src:variants.full!,filename:file.name.replace(/[^a-zA-Z0-9._-]/g,'-'),alt:alt.trim(),width:metadata.width,height:metadata.height,mimeType:'image/webp',size:file.size,order:0,isPrimary:primary,createdAt:now,updatedAt:now,variants}}async delete(image:MediaImage){for(const src of Object.values(image.variants||{full:image.src})){if(!src?.startsWith('/uploads/'))continue;try{await unlink(path.join(process.cwd(),'public',src))}catch{}}}async replace(oldImage:MediaImage,file:File,scope:string,alt:string){const next=await this.upload(file,scope,true,alt);await this.delete(oldImage);return next}async list(){return []}}
export const imageStorage:ImageStorageProvider=new LocalImageStorageProvider();
