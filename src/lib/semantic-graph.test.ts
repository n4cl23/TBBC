import {describe,expect,it} from 'vitest';import {graphService,inheritSemantics,semanticEntities,semanticRelations} from './semantic-graph';
describe('semantic knowledge graph',()=>{
 it('uses canonical ids and has no loops or orphan references',()=>{const ids=new Set(semanticEntities.map((entity)=>entity.canonicalId));expect(new Set(semanticEntities.map((entity)=>entity.canonicalId)).size).toBe(semanticEntities.length);for(const edge of semanticRelations){expect(ids.has(edge.source)).toBe(true);expect(ids.has(edge.target)).toBe(true);expect(edge.source).not.toBe(edge.target);expect(edge.weight).toBeGreaterThanOrEqual(0);expect(edge.weight).toBeLessThanOrEqual(1);expect(edge.canonical).toBe(true)}});
 it('inherits realm semantics without inventing missing dimensions',()=>{const creature=graphService.getEntity('tempest-hawk')!;expect(inheritSemantics(creature)).toMatchObject({kingdom:graphService.getEntity('stormreach')?.canonicalId,politicalAlignment:null,economicContext:null})});
 it('answers canonical relationship queries with bounded traversal',()=>{expect(graphService.related('frost-kingdom').map((entity)=>entity.slug)).toContain('vhaldris');expect(graphService.related('vhaldris',99).length).toBeGreaterThan(0);expect(graphService.timeline('last-dragon-slayer').map((entity)=>entity.slug)).toContain('last-dragon')});
 it('indexes names aliases roles and inherited context',()=>{expect(graphService.search({q:'abyss'})[0]?.entity.slug).toBe('kingdom-of-the-abyss');expect(graphService.search({kind:'creature',kingdom:'stormreach'}).length).toBeGreaterThan(0)});
});
