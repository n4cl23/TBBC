import Link from 'next/link';
import {ArrowUpRight} from 'lucide-react';

export function RealmCrest({name,color}:{name:string;color:string}){const initials=name.split(' ').map(x=>x[0]).join('').slice(0,2);return <svg className="realm-crest" viewBox="0 0 120 140" role="img" aria-label={`Crest of ${name}`}><path d="M14 8h92v80c0 23-18 37-46 47C32 125 14 111 14 88V8Z" fill="none" stroke={color} strokeWidth="3"/><path d="M26 22h68v61c0 18-12 28-34 37-22-9-34-19-34-37V22Z" fill={color} opacity=".14"/><circle cx="60" cy="62" r="24" fill="none" stroke={color}/><text x="60" y="69" fill={color} textAnchor="middle" fontSize="18" fontFamily="serif">{initials}</text></svg>}
export function RealmDestination({realm,href}:{realm:{slug:string;name:string;symbol:string;architecture:string;summary:string;accent:string};href?:string}){return <Link className="realm-destination" href={href||`/reinos/${realm.slug}`} style={{'--realm':realm.accent} as React.CSSProperties}><RealmCrest name={realm.name} color={realm.accent}/><div><span className="eyebrow">{realm.symbol}</span><h2 className="serif">{realm.name}</h2><p>{realm.summary}</p><small>{realm.architecture}</small></div><ArrowUpRight/></Link>}
export function CollectionChapter({collection,index,href,meta}:{collection:{slug:string;name:string;description:string;count:number;status:string;accent:string};index:number;href?:string;meta?:string}){return <Link className="collection-chapter" href={href||`/colecoes/${collection.slug}`} style={{'--chapter':collection.accent} as React.CSSProperties}><span className="chapter-roman">{['I','II','III','IV','V','VI'][index]||index+1}</span><div><span className="eyebrow">{meta||`${collection.count} miniaturas · ${collection.status}`}</span><h2 className="serif">{collection.name}</h2><p>{collection.description}</p></div><ArrowUpRight/></Link>}
