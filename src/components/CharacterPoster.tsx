import Image from 'next/image';import Link from 'next/link';import {ArrowUpRight} from 'lucide-react';import type {MediaImage} from '@/types/media';import {mediaAlt,mediaSrc} from '@/lib/media';
export function CharacterPoster({name,title,quote,image,href,index=0}:{name:string;title?:string;quote:string;image:string|MediaImage;href:string;index?:number}){return <Link className="character-poster reveal" href={href} style={{'--poster-delay':`${Math.min(index,8)*60}ms`} as React.CSSProperties}><Image src={mediaSrc(image,'full')} alt={mediaAlt(image,`Retrato de ${name}`)} fill sizes="(max-width: 700px) 100vw, (max-width: 1100px) 50vw, 33vw"/><div className="poster-veil"/><span className="poster-index">{String(index+1).padStart(2,'0')}</span><div className="poster-copy"><small>{title||'Crônicas de Asterheim'}</small><h2 className="serif">{name}</h2><p>{quote}</p><span className="poster-open">Abrir crônica <ArrowUpRight/></span></div></Link>}
