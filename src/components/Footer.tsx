'use client';import Link from 'next/link';import {usePathname} from 'next/navigation';import {dictionary,isLocale,localizedPath,type Locale} from '@/lib/i18n';export function Footer(){const path=usePathname();if(path.startsWith('/admin'))return null;const first=path.split('/').filter(Boolean)[0],locale:Locale=isLocale(first)?first:'pt-br',copy=dictionary[locale];return <footer className="footer"><div className="container grid"><div><div className="brand serif">THE BLACK BANNER CHRONICLES</div><p className="muted">{copy.tagline}</p></div><div><span className="eyebrow">Codex</span><p><Link href={localizedPath(locale,'artBible')}>Art Bible</Link></p><p><Link href={localizedPath(locale,'print')}>{locale==='en'?'3D Printing':locale==='es'?'Impresión 3D':'Impressão 3D'}</Link></p></div><div><span className="eyebrow">{locale==='en'?'Project':locale==='es'?'Proyecto':'Projeto'}</span><p><Link href={localizedPath(locale,'about')}>{locale==='en'?'About':locale==='es'?'Acerca':'Sobre'}</Link></p><p><Link href={localizedPath(locale,'contact')}>{locale==='en'?'Contact':locale==='es'?'Contacto':'Contato'}</Link></p></div></div></footer>}
