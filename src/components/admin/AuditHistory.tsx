'use client';import {useEffect,useMemo,useState} from 'react';import type {AuditEntry} from '@/types/cms';export function AuditHistory(){const [items,setItems]=useState<AuditEntry[]>([]),[query,setQuery]=useState(''),[entity,setEntity]=useState('');useEffect(()=>{fetch('/api/admin/history').then(r=>r.json()).then(setItems)},[]);const filtered=useMemo(()=>items.filter(x=>(!entity||x.entity===entity)&&JSON.stringify(x).toLowerCase().includes(query.toLowerCase())),[items,query,entity]);return <><div className="admin-toolbar"><input className="admin-input" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Pesquisar histórico"/><select className="admin-select" value={entity} onChange={e=>setEntity(e.target.value)}><option value="">Todas as entidades</option>{[...new Set(items.map(x=>x.entity))].map(x=><option key={x}>{x}</option>)}</select></div>{!filtered.length?<div className="admin-panel">Nenhuma alteração registrada.</div>:<div style={{overflowX:'auto'}}><table className="admin-table"><thead><tr><th>Data</th><th>Usuário / IP</th><th>Entidade</th><th>Operação</th><th>Campos</th></tr></thead><tbody>{filtered.map(x=><tr key={x.id}><td>{new Date(x.timestamp).toLocaleString('pt-BR')}</td><td>{x.actor}<small className="muted" style={{display:'block'}}>{x.ip}</small></td><td>{x.entity}</td><td><span className="badge">{x.operation}</span></td><td>{x.changes.map(c=>c.field).join(', ')}</td></tr>)}</tbody></table></div>}</>}
