'use client';import Image from 'next/image';import {useState} from 'react';import {gallery} from '@/data/content';export function Gallery(){const cats=['Todos',...new Set(gallery.map(x=>x.category))], [active,setActive]=useState('Todos');const items=active==='Todos'?gallery:gallery.filter(x=>x.category===active);return <><div className="filter" aria-label="Filtros da galeria">{cats.map(x=><button key={x} className={active===x?'active':''} onClick={()=>setActive(x)}>{x}</button>)}</div><div className="grid">{items.map(x=><figure className="card" key={x.id}><div style={{position:'relative',aspectRatio:'4/3'}}><Image src={x.image} alt={x.title} fill style={{objectFit:'cover'}}/></div><figcaption><h3 className="serif">{x.title}</h3><span className="badge">{x.category}</span></figcaption></figure>)}</div></>}
