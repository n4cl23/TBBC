'use client';import Link from 'next/link';import {useState} from 'react';import {realms} from '@/data/content';export function AsterheimMap(){const [selected,setSelected]=useState(realms[0]);return <section className="world-map" aria-label="Mapa esquemático interativo de Asterheim"><div className="map-field">{realms.map(r=><button key={r.slug} aria-label={`Selecionar reino ${r.name}`} className={`map-marker ${selected.slug===r.slug?'selected':''}`} style={{left:`${r.position.x}%`,top:`${r.position.y}%`,'--realm':r.accent} as React.CSSProperties} onClick={()=>setSelected(r)} aria-pressed={selected.slug===r.slug}><span aria-hidden="true">{r.name}</span></button>)}</div><div className="map-detail"><span className="eyebrow">{selected.symbol}</span><h3 className="serif">{selected.name}</h3><p className="muted">{selected.summary}</p><p><strong>Arquitetura:</strong> {selected.architecture}</p><Link className="button ghost" href={`/reinos/${selected.slug}`}>Explorar reino</Link></div></section>}
