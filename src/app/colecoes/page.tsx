import {Card,PageHead} from '@/components/UI';import {collections} from '@/data/content';export default function Page(){return <><PageHead eyebrow="Catálogo" title="Coleções" text="Companhias, tavernas, feras e relíquias reunidas em séries narrativas."/><section className="section"><div className="container grid">{collections.map(c=><Card key={c.slug} title={c.name} text={c.description} href={`/colecoes/${c.slug}`} meta={`${c.count} peças · ${c.status}`}/>)}</div></section></>}
