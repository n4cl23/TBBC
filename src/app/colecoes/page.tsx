import {PageHead} from '@/components/UI';import {CollectionChapter} from '@/components/WorldIdentity';import {collections} from '@/data/content';export default function Page(){return <><PageHead eyebrow="Crônicas reunidas" title="Coleções" text="Cada coleção é um capítulo de Asterheim, com matéria, tom e destino próprios."/><section className="section"><div className="container collection-chapters">{collections.map((collection,index)=><CollectionChapter key={collection.slug} collection={collection} index={index}/>)}</div></section></>}
