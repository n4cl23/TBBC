import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {crowns,guardians} from '@/data/content';import {Figure,PageHead,Specs} from '@/components/UI';export function generateStaticParams(){return guardians.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,g=guardians.find(x=>x.slug===slug);return {title:g?.name||'Guardião',description:g?.summary,alternates:{canonical:`/guardioes/${slug}`}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,g=guardians.find(x=>x.slug===slug);if(!g)notFound();const crown=crowns.find(x=>x.name===g.crown);const jsonLd={'@context':'https://schema.org','@type':'CreativeWork',name:g.name,description:g.summary,isPartOf:'The Black Banner Chronicles'};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><PageHead eyebrow={g.title} title={g.name} text={g.summary}/><section className="section"><div className="container grid"><Figure src={`/images/guardians/${g.slug}.png`} alt={`Render do Guardião ${g.name}`}/><div><div className="palette" style={{height:10,background:g.accent}}/><span className="eyebrow">O juramento</span><p className="lead">{g.story}</p><Specs items={[["Coroa",g.crown],["Arma",g.weapon||'—'],["Reino",g.realm],["Elemento",g.element],["Virtude",g.virtue],["Escala",g.scale],["Status",g.status],["Símbolo",g.symbol]]}/></div></div><div className="grid" style={{marginTop:32}}><div className="card"><span className="eyebrow">Direção visual</span><h2 className="serif">Prompt de criação</h2><p>{g.prompt}</p></div><div className="card"><span className="eyebrow">Artefato relacionado</span><h2 className="serif">{crown?.name}</h2><p className="muted">{crown?.concept}</p></div></div></section></>}
