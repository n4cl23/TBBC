import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {crowns,guardians,realms} from '@/data/content';import {GuardianExperience} from '@/components/LegendaryExperience';export function generateStaticParams(){return guardians.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,g=guardians.find(x=>x.slug===slug);return {title:g?.name||'Guardião',description:g?.summary,alternates:{canonical:`/guardioes/${slug}`},openGraph:{title:g?.name,description:g?.summary,images:[`/images/guardians/${slug}.png`]}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,g=guardians.find(x=>x.slug===slug);if(!g)notFound();const crown=crowns.find(x=>x.name===g.crown),realm=realms.find(x=>x.name===g.realm);return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({'@context':'https://schema.org','@type':'CreativeWork',name:g.name,description:g.summary,isPartOf:'The Black Banner Chronicles'})}}/><GuardianExperience guardian={g} crown={crown} realm={realm}/></>}
