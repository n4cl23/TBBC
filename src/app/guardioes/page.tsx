import {PageHead} from '@/components/UI';import {CharacterPoster} from '@/components/CharacterPoster';import {guardians} from '@/data/content';export default function Page(){return <><PageHead eyebrow="Os seis juramentos" title="Guardiões" text="Seis protetores. Seis sacrifícios. Um equilíbrio que não pode ser quebrado."/><section className="section"><div className="container poster-grid">{guardians.map((guardian,index)=><CharacterPoster key={guardian.slug} name={guardian.name} title={`${guardian.realm} · ${guardian.virtue}`} quote={guardian.summary} href={`/guardioes/${guardian.slug}`} image={`/images/guardians/${guardian.slug}.png`} index={index}/>)}</div></section></>}
