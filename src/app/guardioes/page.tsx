import {PageHead} from '@/components/UI';import {CharacterPoster} from '@/components/CharacterPoster';import type {Guardian} from '@/data/content';import {getLocalizedPublishedData} from '@/lib/cms-public';import {requestLocale} from '@/lib/locale-server';import {editorialUi} from '@/lib/editorial-ui';import {localizedPath} from '@/lib/i18n';
export default async function Page(){const locale=await requestLocale(),copy=editorialUi[locale].guardians,guardians=(await getLocalizedPublishedData<Guardian>('guardians',locale)).map(x=>x.data);return <><PageHead eyebrow={copy[0]} title={copy[1]} text={copy[2]} variant="guardians" image="/images/guardians/vaelor.png"/><section id="content" className="section"><div className="container poster-grid">{guardians.map((guardian,index)=><CharacterPoster key={guardian.slug} name={guardian.name} title={`${guardian.realm} · ${guardian.virtue}`} quote={guardian.summary} href={localizedPath(locale,'guardians',guardian.slug)} image={`/images/guardians/${guardian.slug}.png`} index={index}/>)}</div></section></>}
