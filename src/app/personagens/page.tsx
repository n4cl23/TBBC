export const dynamic='force-dynamic';import {Card,PageHead} from '@/components/UI';import {characters} from '@/data/content';import {listCharacterMedia} from '@/lib/media-repository';export default async function Page(){const media=await listCharacterMedia();return <><PageHead eyebrow="Arquivo de figuras" title="Personagens" text="Mercenários, estudiosos, monstros e lendas registrados nas crônicas."/><section className="section"><div className="container grid">{characters.map(x=><Card key={x.slug} title={x.name} text={x.summary} href={`/personagens/${x.slug}`} meta={x.title||x.scale} image={media[x.slug]?.primary||x.image}/>)}</div></section></>}
