import Link from 'next/link';import {livingAtlasCopy,livingAtlasSections} from '@/lib/living-atlas';import {localizedPath} from '@/lib/i18n';import {requestLocale} from '@/lib/locale-server';
export const metadata={title:'Atlas Vivo',description:'Diretório do Atlas Vivo, baseado exclusivamente no Canon publicado.'};
export default async function LivingAtlasPage(){const locale=await requestLocale();return <section className="section"><div className="container living-atlas"><span className="eyebrow">Canon 1.1.0 · CMS publicado</span><h1 className="serif">Atlas Vivo</h1><p>Exploração por registros canônicos, sem completar lacunas com conteúdo genérico.</p><nav className="living-atlas-nav" aria-label="Seções do Atlas Vivo">{livingAtlasSections.map(section=><Link key={section} href={localizedPath(locale,'atlas',`vivo/${section}`)}><strong>{livingAtlasCopy[section].title}</strong><span>{livingAtlasCopy[section].description}</span></Link>)}</nav></div></section>}
