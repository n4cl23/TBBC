import {PageHead} from '@/components/UI';import type {NewsArticle} from '@/data/content';import {getLocalizedPublishedData} from '@/lib/cms-public';import {requestLocale} from '@/lib/locale-server';import {editorialUi} from '@/lib/editorial-ui';
export default async function Page(){const locale=await requestLocale(),copy=editorialUi[locale].news,news=(await getLocalizedPublishedData<NewsArticle>('news',locale)).map(x=>x.data);return <><PageHead eyebrow={copy[0]} title={copy[1]} text={copy[2]}/><section className="section"><div className="container grid">{news.map(x=><article className="card" key={x.slug}><span className="eyebrow">{x.date}</span><h2 className="serif">{x.title}</h2><p className="muted">{x.excerpt}</p></article>)}</div></section></>}
