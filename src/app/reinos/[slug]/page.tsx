import {notFound} from 'next/navigation';import {realms,guardians} from '@/data/content';import {Figure,PageHead,Specs} from '@/components/UI';export function generateStaticParams(){return realms.map(x=>({slug:x.slug}))}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,r=realms.find(x=>x.slug===slug);if(!r)notFound();const g=guardians.find(x=>x.realm===r.name);return <><PageHead eyebrow="Reino de Asterheim" title={r.name} text={r.architecture}/><section className="section"><div className="container grid"><Figure/><Specs items={[["Paleta",r.color],["Símbolo",r.symbol],["Guardião",g?.name||'Desconhecido'],["Arquitetura",r.architecture]]}/></div></section></>}
