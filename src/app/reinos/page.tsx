import {Card,PageHead} from '@/components/UI';import {realms} from '@/data/content';export default function Page(){return <><PageHead eyebrow="Cartografia" title="Reinos de Asterheim" text="Territórios definidos por matéria, memória e forças ancestrais."/><section className="section"><div className="container grid">{realms.map(x=><Card key={x.slug} title={x.name} text={x.architecture} href={`/reinos/${x.slug}`} accent={x.accent} meta={x.symbol}/>)}</div></section></>}
