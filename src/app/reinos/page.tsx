import {PageHead} from '@/components/UI';import {RealmDestination} from '@/components/WorldIdentity';import {realms} from '@/data/content';export default function Page(){return <><PageHead eyebrow="Cartografia viva" title="Reinos de Asterheim" text="Atravesse territórios definidos por matéria, memória e forças ancestrais."/><section className="section"><div className="container realm-destinations">{realms.map(realm=><RealmDestination key={realm.slug} realm={realm}/>)}</div></section></>}
