import type {MetadataRoute} from 'next';import {collections,characters,guardians,realms} from '@/data/content';export default function sitemap():MetadataRoute.Sitemap{const base='https://blackbannerchronicles.com';const staticRoutes=['','universo','colecoes','personagens','guardioes','coroas','reinos','art-bible','impressao-3d','galeria','novidades','sobre','contato'];return [...staticRoutes.map(x=>({url:`${base}/${x}`})),...collections.map(x=>({url:`${base}/colecoes/${x.slug}`})),...characters.map(x=>({url:`${base}/personagens/${x.slug}`})),...guardians.map(x=>({url:`${base}/guardioes/${x.slug}`})),...realms.map(x=>({url:`${base}/reinos/${x.slug}`}))]}
