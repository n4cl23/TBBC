import {NextResponse} from 'next/server';import {graphService} from '@/lib/semantic-graph';
export async function GET(_:Request,{params}:{params:Promise<{slug:string}>}){const {slug}=await params,kingdom=graphService.getEntity(slug);if(!kingdom||kingdom.kind!=='realm')return NextResponse.json({message:'Reino não encontrado.'},{status:404});const nodes=[kingdom,...graphService.getGraph().nodes.filter((node)=>node.semantics.kingdom===kingdom.canonicalId)],ids=new Set(nodes.map((node)=>node.canonicalId));return NextResponse.json({nodes,edges:graphService.getGraph().edges.filter((edge)=>ids.has(edge.source)&&ids.has(edge.target))});}
