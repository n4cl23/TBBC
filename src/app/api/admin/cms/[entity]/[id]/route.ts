import {NextRequest,NextResponse} from 'next/server';import {CMS_ENTITIES,type CmsEntityType} from '@/types/cms';import {deleteCmsRecord,duplicateCmsRecord,getCmsRecord,listVersions,restoreVersion,saveCmsRecord} from '@/lib/cms-repository';import {requestContext,validateAdminMutation} from '@/lib/admin-security';import {validateCanonPayload} from '@/lib/canon-validation';import {cmsPatchSchema} from '@/lib/cms-schemas';
const valid=(value:string):value is CmsEntityType=>(CMS_ENTITIES as readonly string[]).includes(value);
export async function GET(_:NextRequest,{params}:{params:Promise<{entity:string;id:string}>}){const {entity,id}=await params;if(!valid(entity))return NextResponse.json({message:'Entidade inválida.'},{status:404});const record=await getCmsRecord(entity,id);return record?NextResponse.json({record,versions:await listVersions(entity,id)}):NextResponse.json({message:'Não encontrado.'},{status:404})}
export async function PATCH(request:NextRequest,{params}:{params:Promise<{entity:string;id:string}>}){const {entity,id}=await params,error=validateAdminMutation(request);if(error)return NextResponse.json({message:error},{status:error.includes('MB')?413:429});if(!valid(entity))return NextResponse.json({message:'Entidade inválida.'},{status:404});try{const parsed=cmsPatchSchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({message:'Payload inválido.',issues:parsed.error.issues.map(issue=>({path:issue.path,message:issue.message}))},{status:400});const body=parsed.data,context=requestContext(request);if('action'in body&&body.action==='duplicate')return NextResponse.json({message:'Conteúdo duplicado.',record:await duplicateCmsRecord(entity,id,context)});if('action'in body&&body.action==='restore')return NextResponse.json({message:'Versão restaurada.',record:await restoreVersion(entity,id,body.versionId,{...context,reason:body.reason})});const validation=validateCanonPayload(entity,body.data);if(!validation.ok)return NextResponse.json({message:validation.message},{status:400});return NextResponse.json({message:'Conteúdo salvo com sucesso.',record:await saveCmsRecord(entity,{id,...body},context)})}catch(error){return NextResponse.json({message:error instanceof Error?error.message:'Falha ao salvar.'},{status:error instanceof Error&&'status'in error?Number(error.status):400})}}
export async function DELETE(request:NextRequest,{params}:{params:Promise<{entity:string;id:string}>}){const {entity,id}=await params,error=validateAdminMutation(request);if(error)return NextResponse.json({message:error},{status:error.includes('MB')?413:429});if(!valid(entity))return NextResponse.json({message:'Entidade inválida.'},{status:404});try{await deleteCmsRecord(entity,id,requestContext(request));return NextResponse.json({message:'Conteúdo arquivado.'})}catch(error){return NextResponse.json({message:error instanceof Error?error.message:'Falha ao arquivar.'},{status:400})}}
