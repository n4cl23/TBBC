import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {crowns,guardians,realms} from '@/data/content';import {CrownExperience} from '@/components/LegendaryExperience';export function generateStaticParams(){return crowns.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,c=crowns.find(x=>x.slug===slug);return {title:c?.name||'Coroa',description:c?.concept,alternates:{canonical:`/coroas/${slug}`},openGraph:{title:c?.name,description:c?.history}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,c=crowns.find(x=>x.slug===slug);if(!c)notFound();const guardian=guardians.find(x=>x.name===c.guardian),realm=realms.find(x=>x.name===c.realm);return <CrownExperience crown={c} guardian={guardian} realm={realm}/>}
