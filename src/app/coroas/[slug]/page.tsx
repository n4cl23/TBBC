import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {crowns,guardians,realms} from '@/data/content';import {PageHead,Specs} from '@/components/UI';export function generateStaticParams(){return crowns.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,c=crowns.find(x=>x.slug===slug);return {title:c?.name||'Coroa',description:c?.concept}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,c=crowns.find(x=>x.slug===slug);if(!c)notFound();const g=guardians.find(x=>x.name===c.guardian),r=realms.find(x=>x.name===c.realm);return <><PageHead eyebrow="Artefato ancestral" title={c.name} text={c.concept}/><section className="section"><div className="container"><div className="crown-hero" style={{'--crown':c.accent} as React.CSSProperties}><div className="crown-sigil">✦</div><div><span className="eyebrow">{c.element}</span><h2 className="serif">{c.visual}</h2><p className="lead">{c.history}</p></div></div><Specs items={[["Material",c.material],["Elemento",c.element],["Guardião",g?.name||c.guardian],["Reino",r?.name||c.realm]]}/><div className="grid" style={{marginTop:24}}>{['Vista frontal','Vista lateral','Vista traseira','Arte conceitual'].map(view=><div className="card" key={view}><span className="eyebrow">Estudo de forma</span><h3 className="serif">{view}</h3><p className="muted">Imagem oficial em preparação. O aro e a proporção seguem a família das Seis Coroas.</p></div>)}</div><div className="card" style={{marginTop:24}}><span className="eyebrow">Prompt de direção</span><p>{c.prompt}</p></div></div></section></>}
