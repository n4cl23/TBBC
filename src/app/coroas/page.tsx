import {PageHead} from '@/components/UI';import {CharacterPoster} from '@/components/CharacterPoster';import {crowns,guardians} from '@/data/content';import {crownLore} from '@/data/legendary-lore';export default function Page(){return <><PageHead eyebrow="Artefatos ancestrais" title="As Seis Coroas" text="Seis relíquias conscientes. Seis forças. Seis preços que nenhum portador compreendeu por inteiro."/><section className="section"><div className="container poster-grid">{crowns.map((crown,index)=>{const guardian=guardians.find(x=>x.name===crown.guardian);return <CharacterPoster key={crown.slug} name={crown.name} title={`${crown.element} · ${crown.guardian}`} quote={crownLore[crown.slug].quote} href={`/coroas/${crown.slug}`} image={guardian?`/images/guardians/${guardian.slug}.png`:'/images/hero/banner-chronicles.webp'} index={index}/>})}</div></section></>}
