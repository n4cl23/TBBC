import {Card,PageHead} from '@/components/UI';import {crowns} from '@/data/content';export default function Page(){return <><PageHead eyebrow="Artefatos ancestrais" title="As Seis Coroas" text="Uma mesma família de relíquias. Seis matérias, seis forças, seis sacrifícios."/><section className="section"><div className="container grid">{crowns.map(x=><Card key={x.slug} title={x.name} text={`${x.material}. ${x.visual}.`} href="/guardioes" accent={x.accent} meta={`${x.element} · ${x.guardian}`}/>)}</div></section></>}
