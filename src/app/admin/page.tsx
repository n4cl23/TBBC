import Link from 'next/link';import {characters,collections,guardians,crowns,realms} from '@/data/content';export default function Page(){return <><span className="eyebrow">Visão geral</span><h1 className="admin-title">Arquivo de Asterheim</h1><p className="muted">Gerencie o conteúdo visual sem alterar arquivos de código.</p><div className="admin-grid">{[[characters.length,'Personagens','/admin/personagens'],[collections.length,'Coleções','/admin/colecoes'],[guardians.length,'Guardiões','/admin/guardioes'],[crowns.length,'Coroas','/admin/coroas'],[realms.length,'Reinos','/admin/reinos']].map(([n,label,href])=><Link href={href as string} className="admin-panel" key={label as string}><span className="eyebrow">{n}</span><h2 className="serif">{label}</h2><p className="muted">Abrir gerenciamento →</p></Link>)}</div></>}
