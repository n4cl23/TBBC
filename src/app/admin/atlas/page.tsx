import Link from 'next/link';
import {atlasFeatures,atlasLayerLabels,atlasLines,atlasModeLabels,atlasPoints,atlasRealms,type AtlasLayer} from '@/data/atlas';

export default function Page(){
  const layerCounts=Object.fromEntries((Object.keys(atlasLayerLabels) as AtlasLayer[]).map(layer=>[layer,atlasFeatures.filter(feature=>feature.layer===layer).length+atlasLines.filter(line=>line.layer===layer).length]));
  return <><span className="eyebrow">Governança cartográfica</span><h1 className="admin-title">Atlas de Asterheim</h1><p className="muted">Painel das projeções derivadas do Canon. Camadas físicas, políticas, históricas, comerciais e climáticas não criam entidades ou relações.</p><div className="admin-grid"><div className="admin-panel"><span className="eyebrow">{atlasRealms.length}</span><h2 className="serif">Territórios canônicos</h2><p className="muted">Fronteiras e capitais projetadas a partir dos seis reinos.</p></div><div className="admin-panel"><span className="eyebrow">{atlasPoints.length}</span><h2 className="serif">Registros vinculados</h2><p className="muted">Locais, Guardiões, Coroas, criaturas e bosses já existentes.</p></div><div className="admin-panel"><span className="eyebrow">{Object.keys(atlasModeLabels).length}</span><h2 className="serif">Mapas temáticos</h2><p className="muted">Físico, político, histórico, comercial e climático.</p></div></div><section style={{marginTop:32}}><h2 className="serif">Cobertura das camadas</h2><div className="admin-grid">{(Object.entries(atlasLayerLabels) as [AtlasLayer,string][]).map(([layer,label])=><div className="admin-panel" key={layer}><span className="eyebrow">{layerCounts[layer]}</span><strong>{label}</strong><p className="muted">Camada derivada · revisão editorial obrigatória</p></div>)}</div></section><div className="admin-toolbar"><Link className="admin-btn" href="/atlas">Visualizar Atlas</Link><Link className="admin-btn secondary" href="/admin/world">Editar registros no World Database</Link><Link className="admin-btn secondary" href="/admin/reinos">Revisar reinos</Link></div></>;
}
