import {EntityManager} from '@/components/admin/EntityManager';export default function Page(){return <><h1 className="admin-title">World Database</h1><p className="muted">Gerencie o canon, seus eventos, papéis narrativos e aliases sem duplicar entidades.</p>{(['events','factions','guilds','bossEncounters','canonAliases','creatures','locations','artifacts','buildings','battles'] as const).map(entity=><section key={entity} style={{marginBottom:48}}><h2 className="serif">{entity}</h2><EntityManager entity={entity}/></section>)}</>}
