import {EntityManager} from '@/components/admin/EntityManager';export default function Page(){return <><h1 className="admin-title">World Database</h1><p className="muted">Cadastre criaturas, locais, artefatos, facções, construções e batalhas.</p>{(['creatures','locations','artifacts','factions','buildings','battles'] as const).map(entity=><section key={entity} style={{marginBottom:48}}><h2 className="serif">{entity}</h2><EntityManager entity={entity}/></section>)}</>}
