import {EntityManager} from '@/components/admin/EntityManager';export default function Page(){return <><h1 className="admin-title">World Database</h1><p className="muted">Relações semânticas usam autocomplete, validação, prioridade, peso, timeline, aliases e IDs canônicos.</p>{(['semanticRelations','events','factions','guilds','bossEncounters','canonAliases','creatures','locations','artifacts','buildings','battles'] as const).map(entity=><section key={entity} style={{marginBottom:48}}><h2 className="serif">{entity}</h2><EntityManager entity={entity}/></section>)}</>}
