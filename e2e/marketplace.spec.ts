import {expect,test} from '@playwright/test';
test.describe('Marketplace Hub',()=>{
 test('lista canais e abre catálogo dedicado',async({page})=>{await page.goto('/marketplaces');await expect(page.getByRole('heading',{name:'Onde as lendas ganham forma'})).toBeVisible();await page.getByRole('link',{name:/Creality Cloud/}).click();await expect(page).toHaveURL(/\/marketplaces\/creality-cloud$/);await expect(page.getByRole('link',{name:/Abrir perfil oficial/})).toHaveAttribute('target','_blank')});
 test('exibe oferta e Product schema no personagem publicado',async({page})=>{await page.goto('/personagens/black-fang-mercenary');await expect(page.getByRole('heading',{name:'Leve a lenda para a mesa'})).toBeVisible();await expect(page.getByRole('link',{name:/Comprar agora/})).toHaveAttribute('rel','noopener noreferrer');const schema=await page.locator('script[type="application/ld+json"]').textContent();expect(schema).toContain('Product');expect(schema).toContain('USD')});
 test('administra marketplaces e anúncios pelo CMS',async({page})=>{await page.goto('/admin/marketplace');await expect(page.getByRole('heading',{name:'Marketplace Hub'})).toBeVisible();await expect(page.getByText('Creality Cloud',{exact:true})).toBeVisible();await page.getByRole('link',{name:'Gerenciar anúncios'}).click();await expect(page.getByRole('heading',{name:'Anúncios e preços'})).toBeVisible()});
});
