import {expect,test} from '@playwright/test';const character='/personagens/the-last-dragon-slayer';for(const width of [360,390,768,1024,1311,1440,1920])test(`integridade tipográfica do personagem em ${width}px`,async({page})=>{await page.setViewportSize({width,height:900});await page.goto(character);await page.waitForLoadState('networkidle');const result=await page.evaluate(()=>{const viewport=document.documentElement.clientWidth,overflow=document.documentElement.scrollWidth>viewport,headings=[...document.querySelectorAll<HTMLElement>('.artbook-note h2')].map(x=>({text:x.innerText,left:x.getBoundingClientRect().left,right:x.getBoundingClientRect().right,top:x.getBoundingClientRect().top,bottom:x.getBoundingClientRect().bottom})),headingsOutside=headings.some(x=>x.left<0||x.right>viewport+1),headingsOverlap=headings.some((a,i)=>headings.some((b,j)=>i<j&&a.left<b.right&&a.right>b.left&&a.top<b.bottom&&a.bottom>b.top));return {overflow,headingsOutside,headingsOverlap,count:headings.length,headings}});expect(result.count).toBe(3);expect(result.overflow).toBeFalsy();expect(result.headingsOutside).toBeFalsy();expect(result.headingsOverlap).toBeFalsy()});
